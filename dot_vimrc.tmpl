let g:mapleader="\<Space>"
let g:my = {}

" ============================================================
" CORE SETTINGS
" ============================================================

set nocompatible

set nomodeline
set modelines=0

set noswapfile

delmarks!
delmarks A-Z0-9

set path=,**

syntax on
set regexpengine=0

if v:version >= 901
	set jumpoptions=stack
endif

" ============================================================
" COLORSCHEME
" ============================================================

let g:jellybeans_overrides = {
\ 'background': { 'ctermbg': 'none', '256ctermbg': 'none' },
\ 'VertSplit': { 'guifg': '333333' },
\ 'StatusLineNC': { 'guibg': '333333', 'guifg': '999999' },
\ 'StatusLine': { 'guibg': '555555', 'guifg': 'ffffff' },
\}

set t_Co=256

set background=dark
{{- if eq .machine "cloudbreak" }}
colorscheme blade_runner
{{- else }}
if v:version >= 900
	colorscheme quiet
else
	colorscheme quiet_legacy
endif
{{- end }}

" ============================================================
" DISPLAY
" ============================================================

set number
set relativenumber
set cursorline

set list
set listchars=tab:┊\ 
set fillchars=vert:│,fold:-
if v:version >= 900
	set listchars+=leadmultispace:·\ 
	set fillchars=eob:~,lastline:@
end

augroup cursorLineFocus
	autocmd!
	autocmd WinEnter * :set cursorline
	autocmd WinLeave * :set nocursorline
augroup END

" ============================================================
" WINDOWS & SPLITS
" ============================================================

set splitbelow
set splitright

set mouse=a

augroup autoResizer
	autocmd!
	autocmd VimResized * exe "normal \<c-w>="
augroup END

noremap <silent> <C-w>z :tabnew %<CR>

" ============================================================
" STATUS LINE
" ============================================================

set laststatus=2
set statusline=
set statusline+=\ 
set statusline+=%f\ 
set statusline+=%m
set statusline+=%h
set statusline+=%r
set statusline+=%=
set statusline+=%l:%c\ 

" ============================================================
" AUTO-READ
" ============================================================

augroup autoReader
	autocmd!
	autocmd BufEnter,CursorHold,FocusGained,InsertEnter * :checktime
augroup END

set autoread
set updatetime=100

" ============================================================
" FILETYPE-SPECIFIC
" ============================================================

augroup filetypePhp
	autocmd!
	autocmd FileType php :setlocal suffixesadd=.php
augroup END

augroup filetypeTypescript
	autocmd!
	autocmd FileType typescript :setlocal suffixesadd=.ts,.d.ts,.tsx,.js,.jsx,.cjs,.mjs,.json
	autocmd FileType typescript :setlocal formatoptions-=ro
augroup END

" ============================================================
" INDENTATION
" ============================================================

function! ApplyIndentationToBuffer(state)
	if a:state == 'spaces-2'
		setlocal expandtab tabstop=2
	elseif a:state == 'spaces-4'
		setlocal expandtab tabstop=4
	elseif a:state == 'tabs-4'
		setlocal noexpandtab tabstop=4
	endif
endfunction

function! ApplyIndentationAuto()
	let l:detected = DetectIndentation()
	call ApplyIndentationToBuffer(l:detected != '' ? l:detected : g:my.default_indentation)
endfunction

function! CycleIndentation()
	let l:states = ['spaces-2', 'spaces-4', 'tabs-4']
	let l:next_index = (index(l:states, g:my.default_indentation) + 1) % len(l:states)
	let g:my.default_indentation = l:states[l:next_index]
	call ApplyIndentationToBuffer(g:my.default_indentation)
	echo 'Tab style: ' . g:my.default_indentation
endfunction

function! DetectIndentation()
	if search('^\t', 'nw')
		return 'tabs-4'
	elseif search('^  \S', 'nw')
		return 'spaces-2'
	elseif search('^    \S', 'nw')
		return 'spaces-4'
	else
		return ''
	endif
endfunction

augroup indentation
	autocmd!
	autocmd FileType * call ApplyIndentationAuto()
augroup END

filetype plugin on

{{ if eq .machine "countercheck" -}}
let g:my.default_indentation = 'spaces-2'
{{- else -}}
let g:my.default_indentation = 'tabs-4'
{{- end }}

call ApplyIndentationToBuffer(g:my.default_indentation)

set autoindent
set smartindent
set shiftwidth=0
set softtabstop=-1

nnoremap <Leader><Tab> :call CycleIndentation()<CR>

" ============================================================
" TAB COMPLETION
" ============================================================

function! InsertTabWrapper()
		let col = col('.') - 1
		if !col
				return "\<tab>"
		endif

		let char = getline('.')[col - 1]
		if char =~ '\k'
				" There's an identifier before the cursor, so complete the identifier.
				return "\<c-p>"
		else
				return "\<tab>"
		endif
endfunction

inoremap <expr> <tab> InsertTabWrapper()
inoremap <s-tab> <c-n>

" ============================================================
" SEARCH & NAVIGATION
" ============================================================

set hlsearch
set incsearch

nnoremap n nzz
nnoremap N Nzz
nnoremap * *zz

nnoremap <silent> <Leader><Leader> :nohlsearch<CR>

" ============================================================
" QUICKFIX
" ============================================================

set grepprg=git\ grep\ -n\ $*

function! QuickfixFilterInteractive()
	let l:pattern = input('Filter quickfix entries (prefix ! to remove matches): ')
	if empty(l:pattern)
		echo "No pattern entered. Aborting."
		return
	endif

	" If pattern starts with !, invert the match and remove the !
	let l:keep_matches = 1
	if l:pattern[0] ==# '!'
		let l:keep_matches = 0
		let l:pattern = l:pattern[1:]
	endif

	let Matcher = {v ->
				\ (has_key(v, 'filename') && v.filename =~ l:pattern)
				\ || (has_key(v, 'bufnr') && bufname(v.bufnr) =~ l:pattern)
				\ || (has_key(v, 'text') && v.text =~ l:pattern)
				\ }

	let l:filtered = l:keep_matches
				\ ? filter(getqflist(), {_, v -> Matcher(v)})
				\ : filter(getqflist(), {_, v -> !Matcher(v)})

	call setqflist(l:filtered)
	redraw
	echo len(l:filtered) . " quickfix entries kept."
endfunction

function! GrepAndOpenQF()
	let l:query = input('Grep for: ')

	if empty(l:query)
		return
	endif

	execute 'silent! grep! ' . shellescape(l:query)
	redraw!

	if len(getqflist()) > 0
		copen
	else
		echo "No results found."
	endif
endfunction

nnoremap <silent> <Leader>/ :call GrepAndOpenQF()<CR>
nnoremap <silent> <Leader>* :grep! <cword><CR><CR>:copen<CR>
nnoremap <silent> <Leader>cf :call QuickfixFilterInteractive()<CR>
nnoremap <silent> <Leader>c1 :cfirst<CR>
nnoremap <silent> <Leader>co :copen<CR>
nnoremap <silent> <Leader>cc :cclose<CR>
nnoremap <silent> <Up> :cprevious<CR>
nnoremap <silent> <Down> :cnext<CR>
nnoremap <Left> :colder<CR>
nnoremap <Right> :cnewer<CR>

" ============================================================
" GIT
" ============================================================

command! GitDiffDevelopArgs call setqflist(map(systemlist('git diff --name-only origin/develop...'), '{ "filename": v:val, "lnum": 1, "col": 1, "text": "" }')) | copen

" DiffHere: left = <base_ref> blob (scratch), right = local file (focused).
function! DiffHere(...) abort
	" safe optional arg: first arg or default
	let l:base_ref = get(a:000, 0, 'origin/develop')

	" 1) Current buffer path
	let l:cur = expand('%:p')
	if empty(l:cur)
		echoerr "DiffHere: current buffer has no filename"
		return
	endif

	" 2) Directory to run git in (use file dir or cwd)
	let l:dir = fnamemodify(l:cur, ':h')
	if l:dir ==# '' | let l:dir = getcwd() | endif

	" 3) Get repo root (capture stderr)
	let l:cmd = 'git -C ' . fnameescape(l:dir) . ' rev-parse --show-toplevel 2>&1'
	let l:root_out = systemlist(l:cmd)
	if v:shell_error || empty(l:root_out)
		let l:msg = empty(l:root_out) ? 'git failed with no output' : join(l:root_out, "\n")
		echohl ErrorMsg
		echom 'DiffHere: unable to determine git repo root for ' . l:dir . ' -- ' . l:msg
		echohl None
		return
	endif
	let l:root = fnamemodify(l:root_out[0], ':p')

	" 4) Repo-relative path for git show
	let l:rel = substitute(l:cur, '^' . escape(l:root, '\') , '', '')

	" 5) Fetch blob from base_ref (capture stderr)
	let l:showcmd = 'git --no-pager -C ' . fnameescape(l:root) . ' show ' . shellescape(l:base_ref . ':' . l:rel) . ' 2>&1'
	let l:blob = systemlist(l:showcmd)
	if v:shell_error
		let l:errtext = join(l:blob, "\n")
		echohl WarningMsg
		echom 'DiffHere: git show failed: ' . l:errtext
		echohl None
		let l:blob = [''] " ensure we still create an empty buffer for diff
	endif

	execute 'tabnew'

	" 6) Ensure we start by populating the left window with the base blob.
	" Use enew to get a fresh buffer then set it to be scratch and populate it.
	" (We avoid using vsplit first to keep deterministic left/right.)
	execute 'enew'
	setlocal buftype=nowrite bufhidden=wipe noswapfile nobuflisted
	call setline(1, l:blob)
	execute 'file ' . fnameescape('[' . l:base_ref . '] ' . l:rel)

	" 7) Open the local file to the right explicitly (regardless of 'splitright')
	" 'rightbelow vsplit' ensures the new window appears to the right of the current window.
	execute 'rightbelow vsplit'
	execute 'edit ' . fnameescape(l:cur)

	" 8) Move focus to the right window (local file)
	wincmd l

	" 9) Enable diff mode if not already on
	if &diff == 0
		windo diffthis
	endif

	" 10) Jump to first diff hunk (in the right window)
	silent! normal! ]c
endfunction

command! -nargs=? DiffHere call DiffHere(<f-args>)

nnoremap <silent> <Leader>gd :GitDiffDevelopArgs<CR>
nnoremap <silent> <Leader>vd :DiffHere<CR>

" ============================================================
" FILE NAVIGATION
" ============================================================

set wildmenu
set wildmode=longest:full,full
if v:version >= 900
	set wildoptions=pum
endif
set wildignore+=**/node_modules/**,**/dist/**

nnoremap <Leader>gf :e client/<cfile>

nnoremap <Leader>b :b 
nnoremap <Leader>f :find 

{{ if not (eq .machine "countercheck") -}}
nnoremap <Leader>gi :echo "SearchForImports() is only implemented on countercheck for now"<CR>
nnoremap <Leader>yg :echo "Quick GitHub linking is only implemented on countercheck for now"<CR>
{{- end }}

" ============================================================
" CLIPBOARD
" ============================================================

" can i get what i want here by just doing :set clipboard=unnamedplus?
" maybe these leader commands aren't really that useful?
" see https://wiki.archlinux.org/title/Vim#Clipboard for more details
nnoremap <Leader>y "+y
vnoremap <Leader>y "+y

nnoremap <silent> <Leader>y% :let @+ = @%<CR>

" ============================================================
" VIMRC EDITING
" ============================================================

nnoremap <silent> <Leader>e :e ~/.vimrc<CR>
nnoremap <silent> <Leader>v :source ~/.vimrc<CR>

" ============================================================
" MISC
" ============================================================

nnoremap <Leader>dd 0D

inoremap <C-C> <Esc>

nnoremap <Leader>: :%s/<C-R><C-W>//g<Left><Left>

nnoremap <Leader>b/ ?function<CR>V/{<CR>%<ESC>/\%V

{{- if eq .machine "countercheck" }}

" ============================================================
" CHESS PROJECT
" ============================================================

function! GoToFile()
	let l:fname = expand('<cfile>')
	let l:resolved = ResolveImport(l:fname)
	execute 'edit ' . l:resolved
endfunction

function! ResolveImport(fname)
	let l:root = getcwd() . '/client'
	let l:parts = split(a:fname, '/')

	if l:parts[0] == 'chesscom'
		call remove(l:parts, 0)
		call insert(l:parts, 'dist', 1)
		return l:root . '/node_modules/@chesscom/' . join(l:parts, '/')
	endif

	let l:prefixes = ['shared/', '']
	let l:suffixes = ['', '.ts', '.d.ts', '.js', '.vue', '/index.ts', '/index.vue']

	for l:prefix in l:prefixes
		for l:suffix in l:suffixes
			let l:candidate = l:root . '/' . l:prefix . a:fname . l:suffix
			if filereadable(l:candidate)
				return l:candidate
			endif
		endfor
	endfor

	return a:fname
endfunction

function! SearchForImports()
	let l:filename = fnamemodify(expand('%'), ':t:r')

	execute 'silent! grep! import.*' . l:filename
	redraw!

	if len(getqflist()) > 0
		copen
	else
		echo "No results found."
	endif
endfunction

let s:pwd = getcwd()

if s:pwd =~ "Workspace/chess"
	let pathsWeCareAbout = ["/client/web/**", "/client/shared/**", "/client/tests/**", "/client/types/**", "/src/Chess/WebBundle/**", "/src/Chess/CoreBundle/**"]
	let &path = join(map(pathsWeCareAbout, { i, path -> s:pwd .. path}), ',')

	set includeexpr=ResolveImport(v:fname)
	set grepprg=git\ grep\ -n\ $*\ --\ ':!*translations*'\ ':!*js_routes*'\ ':!*release-history.md*'
endif

nnoremap gd "zyiw/import.*<C-R>z<CR>:nohlsearch<CR>f'gf/<C-R>z<CR>
nnoremap gf :call GoToFile()<CR>

nnoremap <silent> <Leader>gi :call SearchForImports()<CR>
nnoremap <silent> <Leader>yg :let @+="https://github.com/ChessCom/chess/tree/develop/" . expand('%') . "#L" . line(".")<CR>
nnoremap <silent> <Leader>C :botright call term_start('bash -c "cd ~/Workspace/chess && ./client/build/bin/build compile ' . get(g:, 'my').entries . '"', { 'term_name': 'compile', 'term_rows': 5, 'term_finish': 'close' })<CR><C-W>p
{{- end }}
